# Copy to .env for local tooling only; never commit real values.
# GitHub Actions uses repository Secrets instead (see .github/DEPLOYMENT.md).

AWS_REGION=us-east-1
# After terraform apply:
ENDPOINT_NAME=twitter-sentiment-hf-ep
